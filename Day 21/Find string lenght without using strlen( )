#include <iostream>
using namespace std;

int main() {
    string s;
    cout << "ENTER A STRING : ";
    getline(cin, s);

    int length = 0;
    while (s[length] != '\0') {
        length++;
    }

    cout << "LENGTH OF STRING IS =  " << length << endl;

    return 0;
}
