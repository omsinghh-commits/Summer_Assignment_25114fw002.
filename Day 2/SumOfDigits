#include<iostream>
using namespace std;

int main(){

    int n,sum=0,d;
    cout<<"Enter the number=";
    cin>>n;

    while(n>0){
        d=n%10;
        sum=sum+d;
        n=n/10;
    }
    cout<<"The sum of digits is ="<<sum<<" ";
    cout<<endl;

    return 0;
}
